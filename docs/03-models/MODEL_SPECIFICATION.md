# 🤖 모델 설명

> ElasticNet 변동성 예측 모델 상세 문서

---

## 1. 모델 개요

### 1.1 선택 모델: ElasticNet Regression

```
┌─────────────────────────────────────────────────────────────────┐
│                     ElasticNet Regression                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  손실 함수:                                                      │
│                                                                  │
│  L(β) = ||y - Xβ||² + α * (ρ||β||₁ + (1-ρ)/2 * ||β||²)         │
│          ─────────    ─────────────────────────────────         │
│            MSE         L1 (Lasso)   +    L2 (Ridge)             │
│                                                                  │
│  하이퍼파라미터:                                                 │
│    • α (alpha) = 0.0005   : 정규화 강도                         │
│    • ρ (l1_ratio) = 0.3   : L1 vs L2 비율                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 왜 ElasticNet인가?

| 모델 | 장점 | 단점 | 사용 이유 |
|------|------|------|-----------|
| **Linear** | 단순, 해석 용이 | 과적합 위험 | 기준선 |
| **Lasso (L1)** | 특성 선택 | 상관 특성 문제 | 일부 특성만 사용 |
| **Ridge (L2)** | 안정적 | 특성 선택 불가 | 모든 특성 사용 |
| **ElasticNet** | L1+L2 장점 결합 | 튜닝 필요 | ⭐ **최종 선택** |

ElasticNet은 **변동성 특성 간 상관관계가 높은** 금융 데이터에 적합합니다.

---

## 2. 하이퍼파라미터

### 2.1 탐색 범위

```python
param_grid = {
    'alpha': [0.0001, 0.0005, 0.001, 0.005, 0.01],
    'l1_ratio': [0.1, 0.3, 0.5, 0.7, 0.9]
}
# 총 25개 조합 탐색
```

### 2.2 최적 파라미터

| 파라미터 | 최적값 | 설명 |
|----------|--------|------|
| **alpha** | 0.0005 | 약한 정규화 (데이터 신호 보존) |
| **l1_ratio** | 0.3 | L2 위주 (70% Ridge + 30% Lasso) |

### 2.3 파라미터 해석

```
alpha = 0.0005 (낮음)
  → 정규화 강도가 약함
  → 데이터의 패턴을 더 많이 학습
  → 변동성 예측에서 과거 패턴이 중요하기 때문

l1_ratio = 0.3 (L2 위주)
  → 30% Lasso (특성 선택) + 70% Ridge (안정화)
  → 모든 변동성 특성이 어느 정도 기여
  → 상관된 특성들의 계수를 공유
```

---

## 3. 특성 (Features)

### 3.1 특성 카테고리 (31개)

```
┌─────────────────────────────────────────────────────────────────┐
│                        특성 카테고리                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [1] 변동성 지표 (Volatility)                                   │
│      • volatility_5, volatility_10, volatility_20, volatility_50│
│      • realized_vol_5, realized_vol_10, realized_vol_20         │
│                                                                  │
│  [2] 래그 변수 (Lag Features)                                   │
│      • vol_lag_1, vol_lag_2, vol_lag_3, vol_lag_5              │
│      • return_lag_1, return_lag_2, return_lag_3                │
│                                                                  │
│  [3] 변동성 비율 (Volatility Ratios)                            │
│      • vol_ratio_5_20 = vol_5 / vol_20                         │
│      • vol_ratio_10_50 = vol_10 / vol_50                       │
│                                                                  │
│  [4] 통계 지표 (Statistics)                                     │
│      • mean_return_5, mean_return_20                            │
│      • skew_20, kurtosis_20                                     │
│      • momentum_5, momentum_10, momentum_20                     │
│                                                                  │
│  [5] 기타                                                       │
│      • high_low_range, volume_ratio                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 특성 중요도 (SHAP 기반)

```
특성 중요도 Top 10:

volatility_20      ████████████████████████████ 1.00
realized_vol_20    ██████████████████████████   0.92
volatility_50      ████████████████████         0.71
vol_lag_1          ███████████████████          0.68
return_lag_1       ██████████████████           0.64
vol_ratio_5_20     █████████████████            0.61
volatility_10      ████████████████             0.57
mean_return_20     ██████████████               0.50
skew_20            █████████████                0.46
momentum_10        ████████████                 0.43
```

### 3.3 특성 상관관계

```
높은 상관 (|r| > 0.8):
  • volatility_20 ↔ realized_vol_20 (r = 0.95)
  • volatility_10 ↔ volatility_20 (r = 0.88)
  • vol_lag_1 ↔ volatility_20 (r = 0.85)

→ ElasticNet의 L2 정규화가 이 상관관계를 처리
```

---

## 4. 학습 과정

### 4.1 데이터 분할

```python
# 시간순 분할 (shuffle=False)
train_size = int(len(data) * 0.8)

X_train = X[:train_size]  # 1,095 샘플
X_test = X[train_size:]   # 274 샘플
```

### 4.2 스케일링

```python
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)

# 스케일링 통계 (학습 데이터 기준)
# mean = X_train.mean()
# std = X_train.std()
```

### 4.3 GridSearchCV

```python
from sklearn.model_selection import GridSearchCV, KFold
from sklearn.linear_model import ElasticNet

model = ElasticNet(random_state=42)
cv = KFold(n_splits=5, shuffle=False)

grid_search = GridSearchCV(
    model, 
    param_grid, 
    cv=cv, 
    scoring='r2',
    n_jobs=-1
)
grid_search.fit(X_train_scaled, y_train)

# 결과
# Best alpha: 0.0005
# Best l1_ratio: 0.3
# Best CV R²: 0.1190
```

---

## 5. 성능 지표

### 5.1 주요 지표

| 지표 | 값 | 설명 |
|------|-----|------|
| **Test R²** | 0.2218 | 테스트 세트 결정계수 |
| **CV R² (Mean)** | 0.1190 | 교차 검증 평균 |
| **CV R² (Std)** | ±0.2520 | 교차 검증 표준편차 |
| **Test RMSE** | 0.0074 | 평균 제곱근 오차 |
| **Test MAE** | 0.0042 | 평균 절대 오차 |

### 5.2 성능 해석

```
┌─────────────────────────────────────────────────────────────────┐
│                        성능 해석                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Test R² = 0.2218 (22.18%)                                      │
│    → 변동성 변동의 22%를 설명                                   │
│    → 금융 예측에서 합리적인 수준                                │
│                                                                  │
│  CV R² < Test R²                                                │
│    → 데이터의 시기별 패턴 차이                                  │
│    → 과적합 아님 (오히려 보수적)                                │
│                                                                  │
│  CV Std = 0.2520 (높음)                                         │
│    → 폴드별 성능 변동 큼                                        │
│    → 시장 상황에 따라 예측력 변화                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 벤치마크 비교

```
┌─────────────┬──────────┬──────────┬──────────────────┐
│    모델     │  Test R² │  RMSE    │      비고        │
├─────────────┼──────────┼──────────┼──────────────────┤
│ HAR         │  0.2150  │  0.0032  │ 학술 표준        │
│ Ridge       │  0.3030  │  0.0053  │ 실험 모델        │
│ ElasticNet  │  0.2218  │  0.0074  │ ⭐ 최종 채택     │
│ Random Forest│ 0.1800  │  0.0085  │ 과적합 경향     │
└─────────────┴──────────┴──────────┴──────────────────┘

선택 이유:
  - HAR 대비 동등하거나 우수한 성능
  - Ridge보다 보수적이지만 안정적
  - Random Forest는 과적합 위험
```

---

## 6. 모델 계수 분석

### 6.1 주요 계수

```python
# 모델 계수 (절대값 기준 Top 10)
coefficients = {
    'volatility_20': 0.342,
    'realized_vol_20': 0.298,
    'vol_lag_1': 0.187,
    'volatility_50': 0.156,
    'vol_ratio_5_20': -0.134,  # 음수: 비율이 높으면 변동성 감소 예상
    'return_lag_1': -0.098,
    'volatility_10': 0.087,
    'skew_20': -0.076,
    'momentum_10': -0.065,
    'mean_return_20': -0.054,
}
```

### 6.2 계수 해석

```
양수 계수 (변동성 증가 예측):
  • volatility_20 (+0.342): 과거 변동성이 높으면 미래도 높음
  • vol_lag_1 (+0.187): 1일 전 변동성이 높으면 지속

음수 계수 (변동성 감소 예측):
  • vol_ratio_5_20 (-0.134): 단기 변동성이 상대적으로 높으면 평균회귀
  • skew_20 (-0.076): 양의 왜도(상승 추세)는 변동성 감소와 연관
```

---

## 7. 모델 저장 및 로드

### 7.1 저장

```python
import joblib

# 모델 저장
joblib.dump(best_model, 'data/models/final_elasticnet.pkl')
joblib.dump(scaler, 'data/models/final_scaler.pkl')

# 예측 결과 저장
predictions_df.to_csv('data/raw/test_predictions.csv', index=False)
```

### 7.2 로드 및 예측

```python
import joblib
import pandas as pd

# 모델 로드
model = joblib.load('data/models/final_elasticnet.pkl')
scaler = joblib.load('data/models/final_scaler.pkl')

# 새 데이터 예측
new_data = pd.read_csv('new_features.csv')
new_scaled = scaler.transform(new_data)
predictions = model.predict(new_scaled)
```

---

## 8. 한계점 및 개선 방향

### 8.1 현재 한계

| 한계 | 설명 | 영향 |
|------|------|------|
| 선형 모델 | 비선형 관계 포착 불가 | 성능 상한 존재 |
| 정적 특성 | 시장 레짐 변화 미반영 | 특정 시기 성능 저하 |
| 단일 자산 | SPY만 학습 | 다른 자산에 일반화 필요 |

### 8.2 개선 방향

```
단기 개선:
  1. Bayesian Optimization으로 하이퍼파라미터 정밀 튜닝
  2. 추가 특성 (VIX, 옵션 데이터) 포함
  3. 앙상블 (ElasticNet + Ridge + Lasso)

장기 개선:
  1. XGBoost / LightGBM 비선형 모델 탐색
  2. LSTM / Transformer 시계열 딥러닝
  3. 레짐 스위칭 모델 (HMM + ElasticNet)
```

---

**문서 작성일**: 2025-12-04
**버전**: 1.0
