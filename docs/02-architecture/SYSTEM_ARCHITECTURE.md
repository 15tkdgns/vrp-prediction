# 🏗️ 시스템 아키텍처

> SPY ETF 변동성 예측 시스템의 기술 아키텍처

---

## 1. 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        SPY 변동성 예측 시스템                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐              │
│  │   데이터     │───▶│   특성 공학   │───▶│   모델 학습   │              │
│  │   수집       │    │              │    │              │              │
│  │  (yfinance)  │    │  (31개 특성)  │    │ (ElasticNet) │              │
│  └──────────────┘    └──────────────┘    └──────────────┘              │
│         │                                       │                       │
│         ▼                                       ▼                       │
│  ┌──────────────┐                       ┌──────────────┐              │
│  │   데이터     │                       │   모델 검증   │              │
│  │   저장소     │                       │  (Purged CV)  │              │
│  │  (CSV/JSON)  │                       │              │              │
│  └──────────────┘                       └──────────────┘              │
│                                                │                       │
│                                                ▼                       │
│                                         ┌──────────────┐              │
│                                         │   대시보드   │              │
│                                         │ (Streamlit)  │              │
│                                         └──────────────┘              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 데이터 파이프라인

### 2.1 데이터 수집 → 전처리 → 특성 생성

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          데이터 파이프라인                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [1] 데이터 수집                                                         │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │  yfinance.download("SPY", start="2015-01-01", end="2024-12-31") │     │
│  └───────────────────────────────────────────────────────────────┘     │
│                                  │                                      │
│                                  ▼                                      │
│  [2] 기본 필드                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Date, Open, High, Low, Close, Adj Close, Volume                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                  │                                      │
│                                  ▼                                      │
│  [3] 파생 특성 생성                                                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  • 수익률: daily_return = log(Close_t / Close_{t-1})             │   │
│  │  • 변동성: volatility_N = std(returns, window=N)                 │   │
│  │  • 래그: vol_lag_k = volatility_{t-k}                            │   │
│  │  • 비율: vol_ratio = vol_short / vol_long                        │   │
│  │  • 통계: skew, kurtosis, momentum                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                  │                                      │
│                                  ▼                                      │
│  [4] 타겟 생성 (시간적 분리 보장)                                       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  target_vol_5d = sqrt(sum(r_{t+1}^2 + ... + r_{t+5}^2))          │   │
│  │                                                                   │   │
│  │  ⚠️ 특성: t 시점 이전 데이터만                                    │   │
│  │  ⚠️ 타겟: t+1 ~ t+5 시점 데이터                                   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 데이터 저장 구조

```
data/
├── raw/                          # 원시 데이터 및 결과
│   ├── spy_data_2020_2025.csv    # SPY 원본 데이터
│   ├── final_model_performance.json
│   ├── test_predictions.csv      # 테스트 예측 결과
│   └── *.json                    # 각종 분석 결과
│
├── training/                     # 학습용 데이터
│   ├── sp500_2020_2024.csv
│   ├── sp500_2020_2024_enhanced.csv
│   ├── sp500_leak_free_dataset.csv
│   └── sp500_ultra_leak_free.csv
│
├── models/                       # 학습된 모델
│   ├── final_elasticnet.pkl
│   └── final_scaler.pkl
│
└── validation/                   # 검증 데이터
    └── comprehensive_model_validation.json
```

---

## 3. 모듈 구조

### 3.1 src/ 디렉토리 상세

```
src/
│
├── __init__.py
├── main.py                       # 메인 진입점
│
├── core/                         # 🔧 핵심 모듈
│   ├── config.py                 # 설정 관리
│   ├── logger.py                 # 로깅 시스템
│   ├── data_processor.py         # 데이터 처리
│   ├── safe_data_processor.py    # 안전한 데이터 처리
│   └── exceptions.py             # 예외 정의
│
├── models/                       # 🤖 예측 모델
│   ├── train_final_reproducible_model.py  ⭐ 메인 학습
│   ├── correct_target_design.py           ⭐ 타겟 설계
│   └── garch_enhanced_model.py            GARCH 통합
│
├── validation/                   # ✅ 검증 시스템
│   ├── purged_cross_validation.py         ⭐ Purged K-Fold
│   ├── economic_backtest_validator.py     경제적 백테스트
│   ├── walk_forward_validation.py         Walk-Forward
│   └── advanced_leakage_analysis.py       누출 분석
│
├── features/                     # 📊 특성 공학
│   ├── advanced_feature_engineering.py
│   ├── engineering.py
│   └── selection.py
│
├── analysis/                     # 📈 분석 도구
│   ├── xai_dashboard_connector.py
│   ├── shap_analysis.py
│   ├── verified_xai_analysis.py
│   └── statistical_significance_tester.py
│
└── utils/                        # 🛠️ 유틸리티
    └── system_orchestrator.py    # 시스템 통합
```

---

## 4. 모델 학습 파이프라인

### 4.1 학습 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          모델 학습 파이프라인                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [1] 데이터 로드                                                         │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  df = pd.read_csv('data/training/...csv')       │                    │
│  │  X = df[feature_columns]                        │                    │
│  │  y = df['target_vol_5d']                        │                    │
│  └─────────────────────────────────────────────────┘                    │
│                          │                                               │
│                          ▼                                               │
│  [2] Train/Test 분할 (시간순)                                           │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  train_size = 0.8                               │                    │
│  │  X_train, X_test = X[:split], X[split:]         │                    │
│  │  (Shuffle 없음 - 시계열 순서 보존)              │                    │
│  └─────────────────────────────────────────────────┘                    │
│                          │                                               │
│                          ▼                                               │
│  [3] 스케일링                                                           │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  scaler = StandardScaler()                      │                    │
│  │  X_train_scaled = scaler.fit_transform(X_train) │                    │
│  │  X_test_scaled = scaler.transform(X_test)       │                    │
│  └─────────────────────────────────────────────────┘                    │
│                          │                                               │
│                          ▼                                               │
│  [4] GridSearchCV (하이퍼파라미터 튜닝)                                  │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  param_grid = {                                 │                    │
│  │      'alpha': [0.0001, 0.0005, 0.001, 0.01],    │                    │
│  │      'l1_ratio': [0.1, 0.3, 0.5, 0.7, 0.9]      │                    │
│  │  }                                              │                    │
│  │  cv = KFold(n_splits=5, shuffle=False)          │                    │
│  └─────────────────────────────────────────────────┘                    │
│                          │                                               │
│                          ▼                                               │
│  [5] 최적 모델 학습                                                      │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  Best: alpha=0.0005, l1_ratio=0.3               │                    │
│  │  model = ElasticNet(**best_params)              │                    │
│  │  model.fit(X_train_scaled, y_train)             │                    │
│  └─────────────────────────────────────────────────┘                    │
│                          │                                               │
│                          ▼                                               │
│  [6] 평가 및 저장                                                        │
│  ┌─────────────────────────────────────────────────┐                    │
│  │  Test R² = 0.2218                               │                    │
│  │  CV R² = 0.1190 ± 0.2520                        │                    │
│  │  joblib.dump(model, 'final_elasticnet.pkl')     │                    │
│  └─────────────────────────────────────────────────┘                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 검증 시스템

### 5.1 Purged K-Fold Cross-Validation

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Purged K-Fold CV 개념도                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  일반 K-Fold:                                                           │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │ Train │ Train │ Train │ Train │ Test │                         │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                      ↑                                  │
│                              데이터 누출 가능!                          │
│                                                                          │
│  Purged K-Fold:                                                         │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │ Train │ Train │ Train │ Purge │ Embargo │ Test │               │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                           ↑       ↑                                     │
│                      5일 제거  5일 금지                                  │
│                                                                          │
│  설정:                                                                   │
│    - n_splits = 5                                                       │
│    - purge_length = 5 (훈련 세트 끝 5일 제거)                           │
│    - embargo_length = 5 (검증 세트 시작 전 5일 금지)                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 경제적 백테스트

```
투자 전략:
1. 예측 변동성이 높으면 → 포지션 축소 (0.5x)
2. 예측 변동성이 낮으면 → 포지션 확대 (2.0x)

백테스트 설정:
- 기간: 2015-04-01 ~ 2024-12-20
- 거래비용: 0.1% (편도)
- 타겟 변동성: 15% (연간)
- 포지션 범위: 0.5x ~ 2.0x
```

---

## 6. 대시보드 아키텍처

### 6.1 Streamlit 대시보드 (app.py)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     Streamlit 대시보드 (6탭)                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────┬─────────┬─────────┬─────────┬─────────┬─────────┐         │
│  │ 변동성  │ 특성    │ 경제적  │ 모델    │ 통계적  │ 특성    │         │
│  │ 예측    │ 영향    │ 가치    │ 비교    │ 검증    │ 분석    │         │
│  └─────────┴─────────┴─────────┴─────────┴─────────┴─────────┘         │
│       │         │         │         │         │         │               │
│       ▼         ▼         ▼         ▼         ▼         ▼               │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                       시각화 컴포넌트                            │   │
│  │  • Plotly 차트                                                   │   │
│  │  • 성능 지표 카드                                                │   │
│  │  • 데이터 테이블                                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 탭별 기능

| 탭 | 기능 | 데이터 소스 |
|-----|------|-------------|
| 변동성 예측 | 실제 vs 예측 변동성 시계열 | test_predictions.csv |
| 특성 영향 | SHAP 기반 특성 중요도 | XAI 분석 결과 |
| 경제적 가치 | 백테스트 성과, 수익 곡선 | backtest_results.json |
| 모델 비교 | ElasticNet vs Ridge vs RF | model_comparison.json |
| 통계적 검증 | 잔차 분석, 유의성 테스트 | 통계 분석 결과 |
| 특성 분석 | 상관관계, 분포 히스토그램 | 원본 데이터 |

---

## 7. 기술 스택 상세

### 7.1 Python 라이브러리

| 라이브러리 | 버전 | 용도 |
|------------|------|------|
| pandas | 2.0+ | 데이터 처리 |
| numpy | 1.24+ | 수치 계산 |
| scikit-learn | 1.3+ | ML 모델, 검증 |
| yfinance | 0.2+ | 금융 데이터 수집 |
| matplotlib | 3.7+ | 정적 시각화 |
| plotly | 5.0+ | 인터랙티브 차트 |
| streamlit | 1.28+ | 대시보드 |
| shap | 0.42+ | 모델 해석 |
| joblib | 1.3+ | 모델 저장/로드 |

### 7.2 재현성 보장

```python
# 랜덤 시드 고정
RANDOM_SEED = 42
np.random.seed(RANDOM_SEED)

# K-Fold CV (셔플 없음)
cv = KFold(n_splits=5, shuffle=False)

# 모델 저장
joblib.dump(model, 'data/models/final_elasticnet.pkl')
joblib.dump(scaler, 'data/models/final_scaler.pkl')
```

---

**문서 작성일**: 2025-12-04
**버전**: 1.0
