# 👥 사용자 가이드

SP500 변동성 예측 시스템 사용법 및 튜토리얼입니다.

## 🚀 빠른 시작

### 1. 시스템 요구사항
- **운영체제**: Windows 10+, macOS 10.15+, Ubuntu 18.04+
- **Python**: 3.10 이상
- **메모리**: 최소 4GB RAM (권장 8GB)
- **디스크**: 최소 2GB 여유 공간

### 2. 설치
```bash
# 1. 저장소 클론
git clone https://github.com/your-repo/sp500-volatility-prediction.git
cd sp500-volatility-prediction

# 2. Python 의존성 설치
pip install -r requirements/base.txt

# 3. 대시보드 의존성 설치 (선택사항)
cd dashboard && npm install
```

### 3. 첫 실행
```bash
# 전체 시스템 실행
PYTHONPATH=/root/workspace python3 src/utils/system_orchestrator.py

# 대시보드 시작
cd dashboard && npm run dev

# 브라우저에서 확인
open http://localhost:8080/index.html
```

## 📊 대시보드 사용법

### 대시보드 접근
1. `cd dashboard && npm run dev` 실행
2. 브라우저에서 `http://localhost:8080/index.html` 접속
3. 자동으로 로딩되는 분석 결과 확인

### 주요 기능

#### 📈 가격 예측 탭
- **SPY 가격 차트**: 실제 가격 vs AI 예측
- **예측 정확도**: 실시간 성능 지표
- **신뢰구간**: 95% 예측 구간 표시

#### 🎯 특성 중요도 탭
- **SHAP 분석**: 각 특성의 기여도
- **특성 순위**: 중요도 순 정렬
- **상호작용 효과**: 특성 간 상호작용

#### 📰 뉴스 영향 탭
- **감정 분석**: 뉴스 감정 점수
- **시장 영향**: 감정과 가격 상관관계
- **트렌드 분석**: 시간별 감정 변화

## 🛠️ 고급 사용법

### 모델 훈련
```bash
# 기본 모델 훈련
PYTHONPATH=/root/workspace python3 src/models/quick_model_training.py

# 고급 모델 훈련 (VMD, LSTM, Transformer)
PYTHONPATH=/root/workspace python3 src/advanced_learning/deep_models.py

# 동적 앙상블 훈련
PYTHONPATH=/root/workspace python3 src/advanced_learning/dynamic_ensemble.py
```

### 백테스팅 실행
```bash
# 실제 거래 비용 반영 백테스팅
PYTHONPATH=/root/workspace python3 src/advanced_learning/financial_backtester.py

# 결과 확인
cat results/backtest_results.json
```

### 대체 데이터 활용
```bash
# FRED 경제 지표 수집
PYTHONPATH=/root/workspace python3 src/advanced_learning/alternative_data.py

# FinBERT 감정 분석
# (자동으로 뉴스 데이터에서 감정 추출)
```

## 📋 일반적인 사용 시나리오

### 시나리오 1: 일일 변동성 예측
**목적**: 다음날 SPY 변동성 예측으로 위험 관리

**실행 방법**:
```bash
# 1. 최신 데이터로 시스템 업데이트
PYTHONPATH=/root/workspace python3 src/utils/system_orchestrator.py

# 2. 예측 결과 확인
cat data/raw/realtime_results.json

# 3. 대시보드에서 시각화 확인
cd dashboard && npm run dev
```

**해석**:
- **변동성 예측값**: 다음날 예상 변동성 (%)
- **신뢰구간**: 95% 예측 구간
- **위험 수준**: 높음/중간/낮음 분류

### 시나리오 2: 포트폴리오 위험 관리
**목적**: 포트폴리오의 동적 헤징 전략 수립

**실행 방법**:
```bash
# 1. 백테스팅으로 전략 검증
PYTHONPATH=/root/workspace python3 src/advanced_learning/financial_backtester.py

# 2. 위험 지표 확인
grep -A 5 "Risk Metrics" results/backtest_results.json

# 3. 최적 헤지 비율 계산
# (백테스트 결과에서 샤프 비율 최대화 비율 확인)
```

**핵심 지표**:
- **VaR (Value at Risk)**: 일일 손실 위험
- **CVaR**: 극단적 손실 시나리오
- **최대 낙폭**: 최악의 연속 손실 기간

### 시나리오 3: 시장 체제 분석
**목적**: 시장 상황 변화 감지 및 대응 전략

**실행 방법**:
```bash
# 1. HMM 시장 체제 분석
PYTHONPATH=/root/workspace python3 src/advanced_learning/alternative_data.py

# 2. 현재 시장 체제 확인
grep "current_regime" data/raw/market_regime_analysis.json

# 3. 체제별 모델 성능 확인
cat results/regime_performance_analysis.json
```

**체제 분류**:
- **상승 추세**: 낮은 변동성, 안정적 상승
- **하락 추세**: 높은 변동성, 지속적 하락
- **횡보 시장**: 중간 변동성, 방향성 없음

## ⚙️ 설정 및 커스터마이제이션

### 기본 설정 변경
```yaml
# config/default.yaml 편집
data:
  symbol: "SPY"           # 다른 ETF로 변경 가능
  lookback_days: 30       # 분석 기간

models:
  ensemble:
    use_gpu: true         # GPU 사용 여부
    cv_folds: 5           # 교차 검증 폴드 수

validation:
  test_size: 0.2          # 테스트 데이터 비율
  purge_days: 2           # 데이터 누출 방지 기간
```

### 환경별 설정
```bash
# 개발 환경 (빠른 테스트)
ENVIRONMENT=development python3 src/main.py

# 운영 환경 (전체 성능)
ENVIRONMENT=production python3 src/main.py
```

## 📊 결과 해석 가이드

### 성능 지표 이해

#### R² Score (결정계수)
- **범위**: -∞ ~ 1.0
- **현재 값**: 0.2136 (21.36%)
- **해석**: 변동성 변화의 21.36%를 모델이 설명

#### 예측 정확도
- **현재 값**: 46.2%
- **기준**: 무작위 예측 50% 대비 우수
- **실용성**: 위험 관리에 충분한 예측력

#### 경제적 가치
- **샤프 비율 개선**: 0.15 (15% 향상)
- **위험 감소**: 18%
- **최대 낙폭 감소**: 12%

### 예측 결과 활용

#### 변동성 예측값 해석
```json
{
  "predicted_volatility": 0.025,
  "confidence_interval": [0.018, 0.032],
  "risk_level": "medium"
}
```

- **예측값**: 2.5% (일일 변동성)
- **신뢰구간**: 1.8% ~ 3.2%
- **위험 수준**: 중간 (포지션 크기 조정 권장)

#### 거래 신호 생성
```python
# 예시: 변동성 기반 포지션 크기 조정
if predicted_volatility > 0.03:
    position_size = base_position * 0.5  # 고변동성 시 축소
elif predicted_volatility < 0.015:
    position_size = base_position * 1.2  # 저변동성 시 확대
```

## 🚨 주의사항 및 제한사항

### 사용 시 주의사항
1. **과적합 주의**: 백테스트 결과가 미래 성과를 보장하지 않음
2. **시장 체제 변화**: 극단적 시장 상황에서는 예측력 저하 가능
3. **거래 비용**: 실제 거래 시 추가 비용 고려 필요

### 시스템 제한사항
- **데이터 지연**: 일일 데이터 기반 (고빈도 거래 부적합)
- **단일 자산**: SPY ETF에 특화 (다른 자산 적용 시 재훈련 필요)
- **시장 시간**: 미국 시장 거래 시간 기준

## 🔧 문제 해결

### 일반적인 문제

#### 1. "ModuleNotFoundError" 오류
```bash
# 해결: PYTHONPATH 설정
export PYTHONPATH=/root/workspace:$PYTHONPATH
# 또는
PYTHONPATH=/root/workspace python3 your_script.py
```

#### 2. 대시보드 접속 불가
```bash
# 포트 충돌 확인
lsof -i :8080

# 다른 포트 사용
cd dashboard && PORT=8081 npm run dev
```

#### 3. GPU 관련 오류
```bash
# CPU 모드로 실행
sed -i 's/use_gpu: true/use_gpu: false/' config/default.yaml
```

### 성능 최적화

#### 메모리 사용량 최적화
```python
# 대용량 데이터 처리 시
import gc

# 불필요한 객체 정리
del large_dataframe
gc.collect()
```

#### 처리 속도 향상
```python
# 병렬 처리 활용
import multiprocessing as mp

n_cores = mp.cpu_count()
# 모델 훈련 시 n_jobs=n_cores 사용
```

## 📞 지원 및 도움말

### 문서 링크
- [📋 아키텍처 가이드](../architecture/) - 시스템 구조 이해
- [📊 성능 보고서](../performance/) - 모델 성능 상세 정보
- [🚀 개발자 가이드](../development/) - 코드 수정 및 확장

### 커뮤니티
- [GitHub Issues](https://github.com/issues) - 버그 리포트 및 기능 요청
- [위키 페이지](../../README.md) - 추가 문서 및 FAQ

---

*더 자세한 정보나 고급 기능 사용법은 [메인 문서](../../README.md)를 참조하세요.*